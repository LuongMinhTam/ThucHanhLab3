import { Text, View } from "react-native"

export default Customer = () => {
    return(
        <View style={{flex: 1, justifyContent: 'center'}}>
            <Text>Customer</Text>
        </View>
    )
}